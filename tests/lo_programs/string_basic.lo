// string_basic.lo — WS-1 smoke fixture. Grammar-corrected to LO-3 by SC, 2026-05-29.
// Expected stdout: "hello" + newline (tests/expected/string_basic.out). main() returns 0.
// Exercises: String literal assignment and the String output path.

class Main ( ) {
    int main() {
        String s;
        s = "hello";
        out.print_string(s);
        out.println();
        return 0;
    }
}
