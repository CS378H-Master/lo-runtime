// string_basic.lo — WS-1 Phase 4 placeholder fixture.
//
// TODO(SC): placeholder LO-3 syntax — validate against planning:lo-3-reference.md
// before the conformance harness consumes it. The stable contract is the
// expected output in tests/expected/.
//
// Intent: print a known string literal, exercising String output.

class Main {
    method main() : int {
        var s : String;
        s = "hello";
        out.printString(s);
        out.println();
        return 0;
    }
}
