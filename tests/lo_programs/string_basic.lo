// string_basic.lo — shared smoke fixture.
//
// Surface syntax validated against planning:lo-3-reference.md. Stable contract:
// expected/string_basic.out (`hello`).
//
// Intent: print a String literal, exercising the String-output path.

class Main ( ) {
    int main() {
        String s;
        s = "hello";
        out.print_string(s);
        out.println();
        return 0;
    }
}
