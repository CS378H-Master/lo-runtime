// alloc_basic.lo — WS-1 smoke fixture. Grammar-corrected to LO-3 by SC, 2026-05-29.
// Expected stdout: "42" + newline (tests/expected/alloc_basic.out). main() returns 0.
// Exercises: object allocation (the Main instance) and the integer output path.

class Main ( ) {
    int main() {
        int n;
        n = 42;
        out.print_int(n);
        out.println();
        return 0;
    }
}
